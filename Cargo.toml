[package]
name = "osm-overpass"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4.43"
reqwest = { version = "0.13.2", features = ["deflate"] }
serde = { version = "1.0.228", features = ["derive"] }
